{
  "name": "lumivox-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static local development server for the Lumivox website.",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8000",
    "start": "npm run dev",
    "preview": "vite --host 127.0.0.1 --port 8000"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
